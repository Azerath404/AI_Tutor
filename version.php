<?php
defined('MOODLE_INTERNAL') || die();

$plugin->component = 'block_ai_tutor'; // Full name of the plugin (used for diagnostics).
$plugin->version   = 2024061000;        // The current plugin version (Date: YYYYMMDDXX).
$plugin->requires  = 2022041900;        // Requires this Moodle version.
$plugin->maturity  = MATURITY_ALPHA;  // This is considered as stable version.
$plugin->release   = 'v1.0';    // Human-friendly version name.